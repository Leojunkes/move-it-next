import styled from 'styled-components';

export const Container = styled.div`
    background: var(--blue);
    display:flex;
    height: 3.2rem;
    color: var(--white);
    align-items:center;
    justify-content:space-between;
    padding:0 30px;
    margin-bottom:5px
`