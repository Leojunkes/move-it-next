import styled from 'styled-components';

export const Container = styled.div`
    background: var(--blue);
    display:flex;
    height: 3.0rem;
    color: var(--white);
    align-items:center;
    margin-top:5px;
    padding-bottom:2px;
    justify-content:space-evenly;
    margin-bottom:5px
`